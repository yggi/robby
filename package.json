{
  "name": "robby-and-funke",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run && vite build && node test/run-smoke.mjs test/smoke.fast.mjs",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "test:smoke": "vite build && node test/run-smoke.mjs test/smoke.fast.mjs",
    "test:full": "vite build && node test/run-smoke.mjs test/smoke.full.mjs",
    "check": "vitest run && vite build && node test/run-smoke.mjs test/smoke.fast.mjs",
    "check:full": "vitest run && vite build && node test/run-smoke.mjs test/smoke.fast.mjs && node test/run-smoke.mjs test/smoke.full.mjs",
    "test:dev-warnings": "vite build --mode development --outDir dist-dev && SMOKE_BUILD=dist-dev/index.html node test/smoke.fast.mjs"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "jsdom": "^30.0.1",
    "svelte": "^5.19.0",
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^3.0.5"
  },
  "version": "1.0.0",
  "description": "A programming puzzle game for children who cannot read yet.",
  "license": "UNLICENSED"
}
